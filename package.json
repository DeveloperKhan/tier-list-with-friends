{
  "name": "tier-list-with-friends",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "dev": "concurrently -n server,client -c blue,green --kill-timeout 1000 \"npm start --prefix server\" \"npm run dev --prefix client\"",
    "dev:server": "npm start --prefix server"
  },
  "devDependencies": {
    "concurrently": "^9.0.0"
  }
}
